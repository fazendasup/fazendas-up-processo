-- Colunas `numeroTorre` / `estruturaOverrideJson`, índice único e backfill em `ensureTorresNumeroEstruturaColumns()` (`server/db.ts`).
-- Correr ADD COLUMN aqui falhava após `ensureBancadas` / multi-projeto já ter preparado a base.

SELECT 1;
