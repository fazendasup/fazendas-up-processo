-- Tabela criada/actualizada por `ensureEstoqueItensTable()` (`server/db.ts`) com `projetoId` e schema atual.
-- Evita ER_TABLE_EXISTS_ERROR quando ensure já criou `estoque_itens` antes do Drizzle marcar esta migração.

SELECT 1;
