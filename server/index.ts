/**
 * Ponto de entrada opcional em desenvolvimento — delega ao servidor canônico.
 *
 * **Entrada oficial:** `server/_core/index.ts` (usado por `pnpm dev`, `pnpm build` e bundle `dist/index.js`).
 * Prefira sempre `pnpm dev` / `pnpm start` em vez de executar este arquivo diretamente.
 */
import "./_core/index";
